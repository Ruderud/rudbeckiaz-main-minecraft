org: ruderud
app: rudbeckiaz-main
service: rudbeckiaz-main-minecraft
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  environment:
    TZ: 'Asia/Seoul'
    # DYNAMODB_TABLE: rudbeckiaz-main-minecraft-room
  # iam:
  #   role:
  #     statements:
  #       - Effect: Allow
  #         Action:
  #           - dynamodb:Query
  #           - dynamodb:Scan
  #           - dynamodb:GetItem
  #           - dynamodb:PutItem
  #           - dynamodb:UpdateItem
  #           - dynamodb:DeleteItem
  #         Resource: 'arn:aws:dynamodb:${aws:region}:*:table/${self:provider.environment.DYNAMODB_TABLE}'

functions:
  entryHandler:
    handler: dist/index.entryHandler
    events:
      - httpApi: '*'

  roomHandler:
    handler: dist/index.roomHandler
    events:
      - httpApi:
          method: '*'
          path: /room

# resources:
#   Resources:
#     RoomDynamoDbTable:
#       Type: 'AWS::DynamoDB::Table'
#       DeletionPolicy: Retain
#       Properties:
#         AttributeDefinitions:
#           - AttributeName: id
#             AttributeType: S
#         KeySchema:
#           - AttributeName: id
#             KeyType: HASH
#         BillingMode: PAY_PER_REQUEST
#         TableName: rudbeckiaz-main-minecraft-room

plugins:
  - serverless-esbuild
